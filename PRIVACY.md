# Branchboard 隐私说明

生效日期：2026 年 7 月 31 日

Branchboard 是本地优先的 Chrome 扩展。扩展不提供反向代理，不调用 OpenAI
API，也不要求用户填写 API Key。

## 数据处理

Branchboard 会在浏览器本地保存用户主动创建或导入的以下内容：

- 画布、节点、提示词和节点关系；
- 用户粘贴、拖入、标注或从当前生成任务回收的图片；
- 浮动画布的位置、尺寸、最小化图标位置和工作模式；
- 当前任务必要的短期状态，例如节点标识和生成结果去重指纹。

这些数据用于恢复工作区、完成用户主动触发的 ChatGPT 网页操作以及把生成结果
放回正确的画布节点。画布内容保存在浏览器的 IndexedDB 中，界面设置保存在
`chrome.storage.local` 中。

## 网络与第三方

自动模式只在用户点击“发送并自动回收”后，操作当前已登录的 ChatGPT 页面。
提示词和参考图片会因此提交给 ChatGPT，并受 OpenAI 自身条款与隐私政策约束。
Branchboard 不会把画布数据发送给开发者或其他第三方，不出售数据，不进行广告
追踪，也不读取 ChatGPT Cookie。

## 权限用途

- `storage`：保存浮窗设置和扩展运行状态；
- `clipboardWrite`：按用户操作复制提示词或图片；
- `offscreen`：在扩展自身的隐藏页面中完成剪贴板写入；
- `chatgpt.com`、`chat.openai.com` 站点权限：显示浮动画布，并在自动模式下
  操作当前 ChatGPT 对话。

扩展不会读取系统剪贴板。

## 保留与删除

数据保留在当前 Chrome 配置中，直到用户删除对应画布数据、清除扩展站点数据或
卸载扩展。卸载扩展后，Chrome 会按照浏览器自身规则删除扩展本地数据。

## 联系

如需咨询隐私问题，请通过发布页面中项目维护者提供的联系渠道沟通。
